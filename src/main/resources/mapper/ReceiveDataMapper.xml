<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="zzzjoy.dao.ReceiveDataMapper">
    <insert id="insertReceiveData" parameterType="zzzjoy.bean.ReceiveData">
        insert into receive_data (code, msg, service, service_ip, data, creat_time)
        VALUES (#{code},#{msg},#{service},#{serviceIp},#{data},#{creatTime})
    </insert>

</mapper>